# Dream2Animation AI — Python dependencies
# Install with: pip install -r requirements.txt

# Core AI
google-generativeai==0.8.3

# Web framework
streamlit>=1.35.0

# HTTP requests (image + music APIs)
requests>=2.31.0

# Audio I/O
soundfile>=0.12.1    # required by Kokoro TTS output
numpy>=1.24.0        # required by Kokoro

# ── TTS engine ────────────────────────────────────────────────────────────────
# Kokoro (torch-based) was tried but is too heavy / dependency-conflicting
# for Streamlit Cloud's free tier — it broke the install entirely.
# Using pyttsx3 (system voices) instead. Requires 'espeak'/'espeak-ng'
# system packages — see packages.txt.
pyttsx3>=2.90

# Logging / utilities (stdlib only, no extra packages needed)