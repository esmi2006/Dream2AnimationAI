# Dream2Animation AI — Python dependencies
# Install with: pip install -r requirements.txt

# Core AI
google-generativeai==0.8.3

# Web framework
streamlit>=1.35.0

# HTTP requests (image + music APIs)
requests>=2.31.0

# Audio I/O
soundfile>=0.12.1    # required by Kokoro TTS output
numpy>=1.24.0        # required by Kokoro

# ── TTS — primary engine: Kokoro (local, free, high quality) ─────────────────
# CPU-only torch build (much smaller download than the default CUDA build,
# important to stay within Streamlit Cloud's build size/time limits).
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
kokoro>=0.7.16
misaki[en]>=0.7.16   # English grapheme-to-phoneme backend used by Kokoro

# ── TTS — fallback engine (used automatically if Kokoro fails to load) ──────
# Requires the 'espeak'/'espeak-ng' system packages — see packages.txt.
pyttsx3>=2.90

# Logging / utilities (stdlib only, no extra packages needed)
